# 生成所有渠道包:
# ./gradlew clean assembleReleaseChannels
# ./gradlew clean assembleDebugChannels
# 生成单个渠道包:
# ./gradlew clean assembleReleaseChannels -PchannelList=huawei
# 生成多个渠道包:
# ./gradlew clean assembleReleaseChannels -PchannelList=huawei,xiaomi
# 生成渠道包&写入额外信息:
# ./gradlew clean assembleReleaseChannels -PchannelList=huawei -PextraInfo=buildtime:20161212,hash:xxxxxxx
huawei
xiaomi
oppo
vivo